version: '3'
services:
  kafka:
    container_name: kafka
    build:
      context: .
      dockerfile: Dockerfile
    image: parrotstream/kafka:2.0.0-5.0.0
    external_links:
    - zookeeper
    ports:
    - 9092:9092
    - 8081:8081
    - 8082:8082
    - 8083:8083
networks:
  default:
    external:
      name: parrot_default
